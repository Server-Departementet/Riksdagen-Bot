# Installed as root's crontab (crontab systemd/cron.root) — reboot needs root.
# Deploys are pushed by the repo's GitHub Actions workflow (.github/workflows/deploy.yml),
# which runs systemd/update.sh --force over SSH.

# Weekly maintenance reboot
25 4    * * 1   /sbin/reboot
